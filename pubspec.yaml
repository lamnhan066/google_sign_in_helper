name: google_sign_in_helper
description: Make it easier for you to use google sign in on all platforms.
version: 0.0.1
homepage: https://github.com/vursin/google_sign_in_helper

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=2.5.0"

dependencies:
  firebase_core: ^2.1.0
  flutter:
    sdk: flutter

  google_sign_in: ^6.0.2
  google_sign_in_dartio: ^0.2.2+1
  http: ^0.13.5
  universal_platform: ^1.0.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
